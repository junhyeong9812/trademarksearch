cluster.name: "trademark-cluster"
network.host: 0.0.0.0

# Elasticsearch 보안 설정 (개발용)
xpack.security.enabled: false

# 메모리 설정
bootstrap.memory_lock: true

# 단일 노드 클러스터 설정 (개발용)
discovery.type: single-node

# 색인 설정
indices.query.bool.max_clause_count: 1024

